import 'package:flutter/material.dart';

class TimeLineTela extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
      ),
    );
  }
}
